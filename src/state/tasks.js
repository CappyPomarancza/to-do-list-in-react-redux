








const initialState = {
    isCompleted: false,
    text: '',
    description: '',
    key: '',
}



export default (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}